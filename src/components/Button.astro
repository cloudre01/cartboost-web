---
interface Props {
  title: string;
  variant?: "primary" | "secondary";
}

const { title, variant = "primary" } = Astro.props;
---

<button
  class="text-white px-4 py-2 font-bold transition duration-200 align-top border-spacing-0.5 bg-blue-600 rounded-lg"
>
  {title}
</button>
